<body>
  <div class="merch_header"> Little Esty Shop</div><br>
  <div class ="merch_nav_button"><%= link_to "Merchant Invoices", "/merchants/#{@merchant.id}/invoices" %></div>
  <div class ="merch_nav_button"><%= link_to "Merchant Items", "/merchants/#{@merchant.id}/items" %></div>
  <div class ="merch_nav_button"><%= link_to "Merchant Dashboard", "/merchants/#{@merchant.id}" %></div>
  <div class ="merch_current_button">Merchant Discounts</div>
  <br><br><br>
  <div class="merch_sub"><h3>Edit Discount Form</h3></div>
  <br><br><br><br>
  
  <%= form_with url: merchant_bulk_discount_path(@merchant, @bulk_discount), method: :patch, local: :true do |m| %>
  <%= m.label :new_disc %>
  <%= m.text_field :new_disc, value: "#{@bulk_discount.discount}" %>
  <%= m.label :new_quantity %>
  <%= m.text_field :new_quantity, value: "#{@bulk_discount.quantity}" %>
  <%= m.submit "Submit" %>
  <% end %>

</body>